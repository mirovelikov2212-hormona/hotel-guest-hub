name: System Sandbox Concurrency Wave 3

on:
  push:
    branches:
      - feat/hotel-lifecycle-v1-intelligence-review
    paths:
      - ".github/workflows/system-sandbox-concurrency-wave3.yml"
      - "scripts/system-concurrency-wave3.mjs"
      - "scripts/vercel-preview-auth-preload.mjs"
      - "app/api/guest/request-create/route.ts"
      - "app/api/guest/day3-survey/route.ts"
      - "app/api/guest/massages/route.ts"
      - "lib/server/supabase-admin.ts"
  workflow_dispatch:

permissions:
  contents: read
  statuses: read
  id-token: write

jobs:
  concurrency-wave-3:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      STAYHUB_SYSTEM_TEST_BASE_URL: https://hotel-guest-hub-git-feat-hote-5f757d-miroslav-velikovs-projects.vercel.app
      STAYHUB_SYSTEM_TEST_PREFIX: factory-heavy-20260901

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Validate concurrency harness syntax
        run: |
          node --check scripts/system-concurrency-wave3.mjs
          node --check scripts/vercel-preview-auth-preload.mjs

      - name: Wait for Vercel Preview on this exact commit
        uses: actions/github-script@v7
        with:
          script: |
            const sha = context.sha;
            for (let attempt = 1; attempt <= 40; attempt += 1) {
              const { data } = await github.rest.repos.getCombinedStatusForRef({
                owner: context.repo.owner,
                repo: context.repo.repo,
                ref: sha,
              });
              const vercel = data.statuses.find((status) => status.context === "Vercel");
              core.info(`Vercel attempt ${attempt}: ${vercel?.state || "pending"}`);
              if (vercel?.state === "success") return;
              if (vercel?.state === "failure" || vercel?.state === "error") {
                core.setFailed(`Vercel Preview failed for ${sha}`);
                return;
              }
              await new Promise((resolve) => setTimeout(resolve, 15000));
            }
            core.setFailed(`Timed out waiting for Vercel Preview for ${sha}`);

      - name: Mint short-lived Vercel trusted OIDC token
        id: oidc
        uses: actions/github-script@v7
        with:
          script: |
            const token = await core.getIDToken();
            core.setSecret(token);
            core.setOutput("token", token);

      - name: Run concurrency and race scenarios
        env:
          STAYHUB_VERCEL_TRUSTED_OIDC_TOKEN: ${{ steps.oidc.outputs.token }}
          VERCEL_AUTOMATION_BYPASS_SECRET: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}
        run: node --import ./scripts/vercel-preview-auth-preload.mjs scripts/system-concurrency-wave3.mjs

      - name: Verify concurrency evidence
        run: jq -e '.accepted == true' system-concurrency-wave3-results.json

      - name: Print concise evidence
        if: always()
        run: |
          if [ -f system-concurrency-wave3-results.json ]; then
            jq '{runId,baseUrl,scenarioSummaries,accepted}' system-concurrency-wave3-results.json
          fi

      - name: Upload Wave 3 evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: system-sandbox-concurrency-wave3-evidence
          path: system-concurrency-wave3-results.json
          if-no-files-found: warn
