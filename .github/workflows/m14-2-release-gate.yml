name: M14.2 Release Gate

on:
  push:
    branches:
      - "audit/m14-2-release-gate-*"

jobs:
  gate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout exact gate commit
        uses: actions/checkout@v4
      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Run full contracts and tenant isolation
        run: npm test
      - name: Run scoped M14.2 lint
        run: >-
          npx eslint
          lib/server/massage-native-runtime.ts
          tests/contracts/m14-native-massage-engine.contract.test.mjs
